# v0.8.0 locked capability-boundary qualification

**Branch:** `release/v0.8.0-evidence-reliability`
**Measurement:** Category-A freeze point, 2026-09-06
**Frozen validator:** 36 independently labelled cases; fixture checksum enforced by the validator.

## Result and contract

The locked validator reports **25 exact semantic divergences**: 24 adjudicated
capability-boundary divergences and one existing reviewed ambiguity. Exact
differences remain visible in the validator; this document does not change a
fixture label or evaluator metric.

| Measure | Result |
| --- | ---: |
| Unsafe under-prioritisation | 0 |
| Severe unsafe under-prioritisation | 0 |
| Incorrect assessed-ticket abstentions | 0 |
| P1 false negatives | 0 |
| Severe ordinary under-prioritisation | 0 |
| P1 false positives | 3 |
| Exact priority / impact / urgency | 23/36 / 27/36 / 29/36 |
| P1 precision / recall | 5/8 / 5/5 |
| Exact semantic divergences | 25 |
| Unadjudicated divergences | 0 |
| Safety blockers | 0 |

The contract distinguishes semantic completeness from operational safety. It
does not call the engine a general natural-language system and does not hide
any exact mismatch.

## Category-A freeze result

The bounded pass resolved `release-05` (historical explanatory request),
`release-15` U5 (observed-use timestamp), and `release-29` (explicitly absent
required-by date). It also corrected current-population projection for
`release-11` and `release-21`, although those cases retain other divergences.
The unreviewed exact count moved **26 → 24**. No Category-A item remains
unadjudicated.

The remaining Category-A hypotheses were reclassified rather than forced:
`release-11`, `release-21`, and `release-25` are Category B; `release-15` and
`release-24` are Category D. In particular, a future panel meeting does not
establish a required-by relationship, and a recurring schedule does not mean
“no deadline”.

## Individually adjudicated residuals

Each row gives the first remaining difference, why it is not a locked-evaluator
safety blocker, and the safe capability or clarification required to resolve
it. Category B is a deliberately deferred moderate deterministic extension;
Category C needs broad cross-clause/compositional understanding; Category D is
an ambiguity that should be clarified rather than inferred.

| ID | Cat. | First exact difference | Why non-safety-blocking | Needed clarification/capability |
| --- | --- | --- | --- | --- |
| 07 | B | I2 impaired → unknown | Actual P4; no unsafe under-priority | Accessibility-equivalence and partial-path model |
| 10 | B | U8 active → unknown | P3 rather than expected P2 is ordinary only | Confirm active finance consequence of costly manual work |
| 11 | B | I1 few-users → team | Current population is retained; 12-person threshold differs | Confirm scope band and explicit stale-display business impairment |
| 17 | B | I1 cohort → unknown | Actual priority remains P1 | Explicitly state the affected lab/process and continuation path |
| 21 | B | U8 active → unknown | Actual priority remains P1 | Confirm whether missing-pay consequence is active now |
| 23 | B | I2 impaired → unknown | P3 rather than expected P2 is ordinary only | Link recoverable rows, rebuild path, and roster process |
| 25 | B | I3 incorrect-data → unknown | Actual priority remains P3 | Moderate recurrence/integrity/repair composition |
| 32 | B | I1 team → unknown | P3 rather than expected P2 is ordinary only | Associate office population and rejected lodgement in one model |
| 35 | B | I3 incorrect-data → unknown | Actual urgency is lower, not unsafe | Explicit data-integrity and containment evidence |
| 09 | C | U7 partial → unknown | Actual P1 is over, not under | Compare attendance fallback with grade-submission equivalence |
| 12 | C | I3 privacy-context → unknown | Actual P3 is over, not unsafe | Infer privacy context and usable summary across clauses |
| 13 | C | I1 team → unknown | Same P3; potential access is conditional | Conditional future population reasoning |
| 19 | C | I3 unavailable → unknown | Actual P1 is over, not unsafe | Join rejected submission, no-paper-route, and statutory harm |
| 20 | C | U5 none → unknown | Actual P3 is over, not unsafe | Separate successful payroll from a report defect and soft timing |
| 22 | C | U6 preference → unknown | Actual P3 is over, not unsafe | Resolved payroll history plus current audit intent |
| 26 | C | I2 impaired → unknown | Actual P1 is over, not unsafe | Propagation, future schedule relation, and no-feed composition |
| 27 | C | I2 impaired → unknown | Same P4; continuation is explicit but compound | Historical/current scope plus CSV-equivalence composition |
| 33 | C | I3 privacy-context → safety | Actual P3 is over, not unsafe | Negated cross-household view and future feature separation |
| 34 | C | I1 team → unknown | Actual P2 is over, not unsafe | Conditional privacy and separate analytics failure composition |
| 14 | D | I4 contained → unknown | Actual priority remains P1 | Ask whether the exposed note is bounded/contained |
| 15 | D | I4 contained → unknown | Actual priority remains P1 | Three observed plans do not prove no further access |
| 24 | D | I4 contained → unknown | Actual priority remains P2 | Ask whether overwrite scope/recovery is contained and required for panel |
| 28 | D | U5 weeks-1-2 → now | Same P3; wording admits competing time anchors | Ask when grant submission is actually required |
| 36 | D | U6 preference → unknown | Same P4 | Ask whether the reference request has a timing preference |

`release-18-campus-drill-roster` remains the separately reviewed acceptable
ambiguity (expected P2, acceptable P3), not a capability-boundary waiver.

## Product boundary and v0.9 direction

First Pass extracts deterministic, local evidence. It works best when a ticket
states the affected population, failed business operation, required-by event,
deadline driver, available path, harm timing, and containment explicitly. It
conservatively leaves relationships unknown when they are implied across
clauses or depend on equivalence judgement.

**v0.9.0 Safe Next Action (documented only):** a next action may use explicit
current primary evidence or analyst-confirmed evidence. It must choose
**Clarify** when business consequence, deadline relation, workaround
equivalence, multi-clause composition, or evidence authority is uncertain. It
must never alter P1–P4.

## Post-v0.8 capability-boundary burn-down

Resolved cases are removed from `tests/release-validation-adjudication.mjs` so a
regression returns as an unadjudicated divergence and fails the release gate
again. This table records what closed each case and which of the eight Decision
Questions it strengthened. The original v0.8.0 measurement above is preserved as
a historical record.

| Date | Case | Cat. | Resolution | Facet |
| --- | --- | --- | --- | --- |
| 2026-09-19 | `release-21-casual-pay-missing` | B | Harm timing recognises absence from a *current* process ("absent from the current pay run") as active harm; past and future clauses remain filtered by the clause temporal classifier. | U8 |
| 2026-09-19 | `release-35-small-group-absence-import` | B | One shared "import skipped records" pattern feeds three dimensions: the data-integrity risk (I3), bounded containment when the skip is limited to one named group (I4), and active harm for records missing now (U8). A register close is an operational deadline driver (U6). A skipped process *step* is not a record-skip, and a spreading skip is not contained. | I3, I4, U6, U8 |
| 2026-09-19 | `release-07-keyboard-accommodation` | B | A keyboard user who "cannot land on" a form control is an action-blocked symptom (I3 unavailable) and an impaired process (I2). "Not an equivalent way" is a partial workaround (U7), and an impaired process with only a partial path is active harm (U8). The ticket routes to the Accessibility domain. A working form is not a barrier. | I2, I3, U7, U8 |
| 2026-09-19 | `release-23-enrolment-restore-lock` | B | A disappeared data batch is data-loss (I3), an impaired process (I2), and active harm (U8); an explicit loss with a usable recovery path is a full workaround (U7). Policy now implements `loss.recoverable` as written: a recovery path caps Impact at Medium but does not stop the clock, so a near restoration need that is not marked soft keeps High Urgency. A loss event that is never stated does not raise restoration urgency. | I2, I3, U7, U8, Impact, Urgency |
| 2026-09-19 | `release-25-timetable-repeating-campus` | B | A weekly cadence that drops the same records is an I4 recurrence with no extra cumulative weight (strongest recurrence evidence now wins in Impact), and the dropped records raise the data-integrity risk (I3). A repair-before-classes routine is an operational driver (U6) and a full workaround (U7); the next scheduled run is a schedule, not a required-by (U5 none). A weekly schedule with no failure verb is not a recurrence. | I3, I4, U5, U6, U7 |
| 2026-09-19 | `release-32-retired-phone-workaround` | B | Counted offices are a team-scale population (I1). A refusal to lodge attendance adjustments is a blocked process (I2) and an action-blocked symptom (I3 unavailable); "rejects every attempt" leaves no workaround (U7), so the blocked process is active harm (U8). A state-adverb "now" before a modal is not an immediate deadline (U5 today). When the action-blocked symptom and the blocked process quote the same wording, Impact counts that evidence once. The retired historical route is not current workaround evidence. | I1, I2, I3, U5, U7, U8, Impact |
| 2026-09-19 | `release-17-lab-eyewash-control` | B | Students using the affected room, lab or space are a cohort-scale population (I1). A dry or failed eyewash or safety control blocks safe use of that space (I2), and "no replacement unit on site" leaves no workaround (U7), so the blocked safety process is active harm (U8). A students mention without a space reference sets no scope. | I1, I2, U7, U8 |
| 2026-09-19 | `release-10-invoice-entry-cost` | B | Manual effort measured in hours (in a manual-work context) is a workaround cost (U7). A failing finance process with costly manual work before a committed operational close is High urgency (U6/urgency) and active harm (U8), because the work is already under way; Impact stays Medium. An impaired process now counts as active failure for the costly-workaround *floor*, and a duration with no manual work is not a cost. | U7, U8, Urgency |
| 2026-09-19 | `release-11-stale-bus-arrivals` | B → reviewed label defect | Stale-display evidence now feeds three dimensions: a board still showing yesterday's values is an impaired process (I2) and a data-integrity risk (I3 incorrect-data), and a live accurate page or coordinator-posted current times is a full workaround (U7). A current board showing yesterday's values is no longer classified historical. The remaining I1 divergence is a **reviewed scope-band label defect**: twelve pupils is `team` per the counting band and three checked-in fixtures (the stale-display scope test, facet `i1-spelled-number-team`, corpus `realistic-wrong-year-level-live-register`), while the frozen locked label says `few-users`; the engine band is retained. | I2, I3, U7, I1 label |
| 2026-09-19 | `release-28-certificate-grant-window` | D | A portal that "works now" is a state, not an immediate deadline, and "in eight/ten days" is a one-to-two-week bucket. A replaced certificate with a future requirement is pending harm, not active harm. The case is now exact. | U5, U8 |
| 2026-09-19 | `release-09`, `release-26`, `release-27` | C | Phase 3 composition pass. `release-09`: a "covers X but cannot do Y" alternative is a partial workaround, and a consequence word inside a system name ("assessment portal") is no longer an imminent-consequence escalation; the case is now exact. `release-26`: "still being copied" is active propagation, a bad mapping is a data-integrity risk, an operational "managers need tomorrow" need is the driver, and propagation-only urgency is bounded at Medium as written in v0.8; the case is now exact. `release-27`: an empty view is unavailable and an impaired process, "everyone else is unaffected" is comparator containment, a complete CSV extract is a workaround, and the continuity-branch deadline demotion no longer applies to outage-grade symptoms; the case is now exact. Also: `would like` is a preference driver, a failing analytics job is impaired, "can use the CSV extract" is a workaround, and forward negation no longer cancels a phrase when a different comparator group is "unaffected". | I2–I4, U6–U8, negation |

Current locked measurement after this burn-down: **13 exact semantic
divergences** — 11 capability boundaries (B=0, C=7, D=4), one acceptable
ambiguity, and one reviewed scope-band label defect; zero unadjudicated
divergences, zero safety blockers, release safety gate PASS.

The seven remaining C cases are documented policy/label boundaries with
clarification coverage tested: `release-12` (preference-driven report request:
privacy context and low impact), `release-13` (conditional future population),
`release-19` (multiple-schools scope weight against a medium-impact label),
`release-20` (explicit can-wait against the facet corpus convention),
`release-22` (corrected-history composition), `release-33` (passive safety
topic does not lower impact), and `release-34` (temporary CSV window driver).
Each is either an over-prioritisation (safe) or a label convention, not an
unsafe under-prioritisation.
