# v0.9.0 Safe Next Action qualification

**Candidate baseline:** v0.8.0 `34e62e6`
**Release branch:** `release/v0.9.0-safe-next-action`

## Qualification contract

Safe Next Action is an additive advisory output. Qualification requires the frozen v0.8 corpus, Impact, Urgency, Priority, priority matrix, privacy scan, and locked validator safety gates to remain unchanged. Any behavioural change must be attributable only to the new advisory result/UI/release metadata.

## Baseline recorded before implementation

- `npm test`: 895 passed, 0 failed.
- `npm run test:release`: PASS; locked 36-case validator has 25 exact semantic divergences (24 capability boundaries and one reviewed ambiguity), with zero unsafe under-prioritisation, severe unsafe under-prioritisation, assessed abstentions, P1 false negatives, unadjudicated divergences, and safety blockers.

## v0.9 direct policy coverage

The shared Node/browser suite has direct structured tests for all six actions, all required Clarify uncertainty classes, inferred-only consequential evidence, analyst-confirmation transitions, no destructive/system-specific instruction, and independent adversarial wording. The release candidate must re-record final counts and locked-validator values after all documentation and metadata changes.

## Final local qualification

- `npm test`: 928 passed, 0 failed; catalogue reconciliation 206/206; 81-case
  evaluator remains at 74/79 exact Priority, Impact, and Urgency with zero
  unreviewed mismatches.
- `npm run test:release`: PASS; locked result remains 25 exact divergences,
  24 capability boundaries, one reviewed ambiguity, and zero safety blockers.
- JavaScript syntax checks and `git diff --check`: PASS.
- The static privacy scan remains clean. No priority fixture changed.

## Browser-runtime qualification

- The earlier Windows launch failed before application execution: its intended
  `C:\\tmp\\chrome-v09-verify` profile directory did not exist and Chrome reported
  a `ProcessSingleton` access failure against the ordinary user profile. The
  browser command was therefore not safely isolated.
- Requalified using Chrome 152.0.7977.76, a freshly created writable GUID
  directory under `%TEMP%`, explicit `--user-data-dir`, and a local
  `127.0.0.1` static HTTP server. Chrome created and used only that disposable
  profile; no interactive Chrome process or normal profile was changed.
- The HTTP-served ES-module application loaded; the Safe Next Action card
  rendered Clarify (P3), Investigate (P2), and analyst-confirmed Contain (P1).
  Analyst refinement recalculated the existing priority display immediately.
- The browser test page reported 928 passed and 0 failed. CDP recorded no page
  console errors/warnings, no uncaught runtime exceptions, and no non-local
  requests. A visual screenshot confirmed the Safe Next Action card and matrix
  rendering. The server, Chrome process tree, and both disposable profiles were
  removed after qualification.
